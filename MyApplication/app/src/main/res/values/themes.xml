<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApp.Splash" parent="Theme.SplashScreen">
        <!-- Background color for splash screen -->
        <item name="windowSplashScreenBackground">#FFFFFF</item>

        <!-- Icon or animation shown in the center -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/mylogoapple</item>

        <!-- Duration of the icon fade-out animation -->
        <item name="windowSplashScreenAnimationDuration">300</item>

        <!-- Theme to apply after splash screen finishes -->
        <item name="postSplashScreenTheme">@style/Theme.Week10_2</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>